# build output
backend/build/
backend/.gradle/
frontend/node_modules/
frontend/dist/

# local runtime data (clue cache + proxied images); anchored on purpose, an
# unanchored "data/" also matches source packages named data
/data/
/backend/data/

# deployment settings (see .env.example)
.env

# editors / os
.idea/
.vscode/
.DS_Store
*.log
